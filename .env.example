# M2 server-only configuration names. Keep real values out of Git and chat.
# These variables are read only by server-side sync/probe modules.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ESSENCE_GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON=
